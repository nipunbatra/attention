Foundation job for rebuilding the four vision parts of an interactive teaching series. Work ONLY inside /Users/nipun/git/attention/src (write assembled outputs to the repo root only when a task says so). No network; no package installs; do not commit or push. Read, in this order: HANDOVER.md (project entry, build and test commands), VISION_FEEDBACK.md (why the vision parts are weak; what to build), VISION_AXES.md (the design you are implementing; authoritative), AXES.md (how the language toy was designed with names first), CONTRACT.md (the AT runtime and components), PARTS.md, and the current vision runtime: vision-scene.js, part5.js, part5-learning.js, part5-diagrams.js, toy5.json, and check_vision1.mjs.

DELIVERABLES (this job builds the shared foundation only; the four parts are rebuilt by later jobs):
1. The scene: implement scenes A, B, C, D, E exactly as VISION_AXES.md section 1 gives them, in a new module vision-shared.js (plain script extending window.AT with AT.vision.scenes, AT.vision.regionOf, AT.vision.patchify(scene) -> 16 raw rows, AT.vision.ramp(value) -> the single grayscale colour). Keep vision-scene.js (the photo hook) untouched.
2. The Vision I toy v2 exactly per VISION_AXES.md section 2: write make_vision_toy.py (numpy, seeded) that builds W_patch from the axis names (fixed), positions, the sparse initial W_Q/W_K/W_V/W_O and the head, trains positions + W_Q/W_K/W_V/W_O + head on scenes A and B (W_patch fixed so the names stay true), verifies targets T1..T5, and writes toy5.json with: d_model 4, d_k 2, d_v 2, "axes" {"e": [...], "qk": [...], "v": [...], "short": {...}}, "scenes" (A..E pixel arrays and names), "initial" parameters, "trained" parameters, the training curve, the attention rows of CLS before/after for A, B, C, the predictions, and a "notes" string. Back up the old file as toy5_v1.json. Update part5.js so AT.vision.forward/attention run on the new toy (16 patches + CLS = 17 rows) and expose AT.vision.axes; keep the API names that sections5 currently call (forward, attention, patchify, editImage, table, vector, diagram) working or provide shims, because the Vision I rebuild job will replace the sections later. Update check_vision1.mjs to the new toy and make it pass.
3. The shared figure components of VISION_AXES.md section 3 in vision-shared.js: grid, thumb, overlay, scatter, circle, triptych, curve, plus the table conventions (thumbnail row labels). Use the shell's design tokens (--c-a rose for attention heat, --c-e blue for e points, --c-d green for Δe arrows, --ink-3 for grid lines); SVG only; every component accepts an `into` element and returns the element; overlay must expose setAlpha(newAlpha) and an onHover callback; scatter must accept axis names for its two axes and draw the arrows as Part 2's 2-D plot does; circle must have animate(states) stepping through an array of point sets. Add a section to CONTRACT.md ("Vision components") documenting each with a copy-paste example, and add vision-shared.js to assemble.py so it is injected for parts 5 to 8 after shared.js and before partN.js.
4. Update sections/sec00_demo.html? No: instead write sections5/sec00_vision_demo.html (not shipped; assemble picks two-digit names only) that exercises every new component on scene A with the new toy: grid with patch lines and names, the overlay for CLS before and after training with hover, the scatter of the 16 patch embeddings with the CLS Δe arrow, a circle with six points and a 3-state animation, a triptych, a curve. Test it: python3 assemble.py --part 5 --only sections5/sec00_vision_demo.html --out t5_demo.html && node qa.mjs t5_demo.html --full --shot t5_demo.png (and --width 390); read the screenshot and iterate until it looks polished (legible names, no clipping, projector-sized type).
5. Sanity for the existing pages: python3 assemble.py --part 5 --out ../vision1.html must still build with zero errors in node qa.mjs (the old sections may show the new numbers; that is acceptable for this job, the rebuild job replaces them). Do not touch sections6/7/8, toy6/7/8, or part6/7/8.js in this job.
Prose rules for any text: plain verbs, sentence case, no em or en dashes, no forced triples. Final report: the axis names and the trained numbers (CLS attention rows before/after for A, B, C and the predictions), files created or changed, the CONTRACT section you added, tests run with results, anything left undone.

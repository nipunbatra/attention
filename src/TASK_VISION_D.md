Rebuild Vision III of an interactive teaching series so it teaches as well as Part 2 (attention.html), which the instructor likes most: gentle introduction, strong motivation, lots of intuition, many good figures, worked examples, and one end-to-end run. Work ONLY inside /Users/nipun/git/attention/src and write the assembled output to /Users/nipun/git/attention/vision3.html. No network; no package installs; do not commit or push. Do not edit shell.html, shared.js, vision-shared.js, toy5.json, part5.js or any other part's sections; if you need a shared component change, add it to part7.js instead and note it in your report.
Read, in this order: HANDOVER.md (entry point, build and test commands), VISION_FEEDBACK.md (the diagnosis and the per-part plan; section 4 is your section list), VISION_AXES.md (the scene, the named axes, the shared components; authoritative), CONTRACT.md (the AT runtime; read the new "Vision components" section and the tables/worksheets/present-mode sections), PARTS.md, BRIEF.md sections 1, 3, 5, humanizer/SKILL.md (prose rules), then the Part 2 reference fragments to copy idioms from: sections/sec01.html (opening with a question and bars), sec05.html (the mock search UI), sec07.html (records view), sec08.html (routing tree with worksheets), sec09.html (the update centrepiece), sec10.html (the 2-D plot with Δe arrows), sec15.html (the stepper walkthrough), sec18.html and sec19.html (pause-and-think and summaries). Then the current sections7/ fragments and part7.js (raw material; rebuild rather than patch).
TARGET: Vision III: CLIP (7 to 9 sections), 3,500 to 4,500 words, at least one figure per section that is not a table or a flow box, one end-to-end run with a loss curve, one caveat box after the hero and no caveat paragraphs at the end of sections, named column headers everywhere (no "coordinate N"), thumbnails as row labels, every number computed by the toy, frames with data-build and text/x-notes on every frame (PRESENT.md), companion prose of 80 to 120 words per section, a motivation opener with one memorable number, the scope line in the hero provenance.
SPECIFIC PLAN: First rebuild toy7.json and part7.js per VISION_AXES.md section 4: image encoder = the frozen Vision I encoder's updated CLS row followed by a learned 4×3 map to the joint axes mug/book/plant; text encoder = bag of words with rows named by those axes; pairs (A, two mugs on a table), (D, a book on a table), (E, a small plant); train 60 steps in a seeded numpy script train_vision3.py with finite-difference checks; export the trajectory of all six unit vectors every step for the animation. Then follow the Vision III plan in VISION_FEEDBACK.md section 4 (sections 1 to 9: the fixed menu versus the open caption with the crane example first; the pairs with the scene thumbnails and named axes; encode and normalise with the unit-circle picture; the similarity matrix with thumbnails on rows and caption text on columns; both directions; training animated on the circle with the loss curve; the batch-size slider from 3 to 8 pairs and the collapse picture; zero-shot with prompts showing a real effect; scope box and summaries).
Update part7.json (title, chain, sections, notation "vision3" rows for the new symbols, provenance line) and the notation rows in part7.js. Build and test: python3 assemble.py --part 7 --out ../vision3.html; node qa.mjs ../vision3.html --width 1280 --height 720 and --width 390 (zero errors, overflowX false); node sweep.mjs ../vision3.html (no problems); node mathdiff.mjs sections7/secNN.html sections7/secNN.html for every NN (no humanizer flags); a present-mode walk (walk.mjs) with no overflow; run the part's check script (check_vision3.mjs; update it to the new toy) and make it pass. If the bundled headless Chromium cannot launch in your sandbox, verify in the system browser (serve the repo root with python3 -m http.server) and say so. Inspect at least six sections visually and fix layout. Final report: sections and frame counts, the key numbers (attention rows, predictions, losses), files created or changed, tests run with results, anything left undone.

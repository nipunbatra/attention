Rebuild Vision I of an interactive teaching series so it teaches as well as Part 2 (attention.html), which the instructor likes most: gentle introduction, strong motivation, lots of intuition, many good figures, worked examples, and one end-to-end run. Work ONLY inside /Users/nipun/git/attention/src and write the assembled output to /Users/nipun/git/attention/vision1.html. No network; no package installs; do not commit or push. Do not edit shell.html, shared.js, vision-shared.js, toy5.json, part5.js or any other part's sections; if you need a shared component change, add it to part5.js instead and note it in your report.
Read, in this order: HANDOVER.md (entry point, build and test commands), VISION_FEEDBACK.md (the diagnosis and the per-part plan; section 4 is your section list), VISION_AXES.md (the scene, the named axes, the shared components; authoritative), CONTRACT.md (the AT runtime; read the new "Vision components" section and the tables/worksheets/present-mode sections), PARTS.md, BRIEF.md sections 1, 3, 5, humanizer/SKILL.md (prose rules), then the Part 2 reference fragments to copy idioms from: sections/sec01.html (opening with a question and bars), sec05.html (the mock search UI), sec07.html (records view), sec08.html (routing tree with worksheets), sec09.html (the update centrepiece), sec10.html (the 2-D plot with Δe arrows), sec15.html (the stepper walkthrough), sec18.html and sec19.html (pause-and-think and summaries). Then the current sections5/ fragments and part5.js (raw material; rebuild rather than patch).
TARGET: Vision I: An image becomes a sequence (8 to 11 sections), 3,500 to 4,500 words, at least one figure per section that is not a table or a flow box, one end-to-end run with a loss curve, one caveat box after the hero and no caveat paragraphs at the end of sections, named column headers everywhere (no "coordinate N"), thumbnails as row labels, every number computed by the toy, frames with data-build and text/x-notes on every frame (PRESENT.md), companion prose of 80 to 120 words per section, a motivation opener with one memorable number, the scope line in the hero provenance.
SPECIFIC PLAN: toy5.json is the rebuilt Vision I toy from the foundation job (16 patches + CLS, named axes brightness/edge/row/col). Follow the Vision I plan in VISION_FEEDBACK.md section 4 exactly: sections 1 to 11 as listed there (Why patches? with the tokens-per-image slider and the 2.5-billion number; the scene; named patch embeddings with the 2-D scatter; position and CLS with the swap check; routing with the records view, the routing tree with thumbnails and the attention overlay before training; values and the update with the mixTable and the CLS Δe arrow on the scatter; prediction; learning with the loss curve and the overlay before/after and the scene C probe; what a trained model attends to; scale-up; pause-and-think and three summaries). Keep the photo hook (vision-scene.js) only in the scene section. Put the motif at the top. Reuse Part 2 idioms: records cards, dotTable/dotCalc worksheets, mixTable with thumbnail row labels, a stepper walkthrough of the full CLS update over the 16 keys.
Update part5.json (title, chain, sections, notation "vision1" rows for the new symbols, provenance line) and the notation rows in part5.js. Build and test: python3 assemble.py --part 5 --out ../vision1.html; node qa.mjs ../vision1.html --width 1280 --height 720 and --width 390 (zero errors, overflowX false); node sweep.mjs ../vision1.html (no problems); node mathdiff.mjs sections5/secNN.html sections5/secNN.html for every NN (no humanizer flags); a present-mode walk (walk.mjs) with no overflow; run the part's check script (check_vision1.mjs; update it to the new toy) and make it pass. If the bundled headless Chromium cannot launch in your sandbox, verify in the system browser (serve the repo root with python3 -m http.server) and say so. Inspect at least six sections visually and fix layout. Final report: sections and frame counts, the key numbers (attention rows, predictions, losses), files created or changed, tests run with results, anything left undone.

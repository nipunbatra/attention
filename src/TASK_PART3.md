Build Part 3 of a three-part single-file interactive teaching series (deep-learning students) in this repository. Work ONLY inside src/ and write the assembled output to the repository root as part3.html. No package installs; do not commit or push. Another job may be editing src/sections/sec05.html, sec06.html and the shared runtime at the same time: do not edit src/sections/*, src/shell.html or src/shared.js; if you need a runtime addition, put it in src/part3.js (loaded after shared.js for Part 3 only).
Read first, in this order: HANDOVER.md (entry point, build and test commands, rules), PARTS.md (multi-part contract: assemble.py --part 3 uses sections3/, toy3.json, part3.json, part3.js), GUIDE3.md (the section plan for Part 3, authoritative), CONTRACT.md (runtime API: tables with named axes, dotCalc/matVecCalc worksheets, netSketch, motif, stepper, reveal, callout, present-mode authoring with .frame/data-build/x-notes), BRIEF.md sections 1, 3, 5, CODEX_FEEDBACK.md items D3 and D5 (heads and layers wording), and the Part 2 reference: toy_report.md (numbers, axes), sections/sec07.html, sec09.html, sec11.html, sec16.html (idioms to copy), sections3_seed/sec17.html and sec18.html (the old heads and layers sections: raw material for Part 3 s07 and s10; they use OLD toy widths and ids s17/s18, so renumber and re-check every shape against the current toy: d_model 5, d_k 3, d_v 2).
Deliverables:
1. toy3.json = toy.json plus a "training" block computed offline with numpy in train_part3.py (write a clean reverse-mode autodiff or reuse the one in make_toy.py if present): for the river sentence "The fisherman sat beside the river bank and watched the" with target "water" after position 10: loss before; gradients of every parameter (E rows used, position rows used, W_Q, W_K, W_V, W_O, W_vocab, b_vocab); parameters after one SGD step for eta in [0.05, 0.1, 0.3]; p(target) and loss after each; and the parallel version (all ten positions with shifted targets under the causal mask, mean loss before and after one step at eta 0.1, per-position losses). Verify gradients on two parameters with finite differences. Numbers to 4 decimals in the JSON.
2. part3.js extending window.AT: AT.train (accessors for those numbers), AT.ln(row, eps) (LayerNorm live), AT.ffn (a fixed illustrative 5 -> 8 -> 5 network with hand-picked one-decimal weights, ReLU), AT.heads (three illustrative head weight rows for "Priya opened the notebook ; she smiled" and for the short river sentence, labelled illustrative, plus the causal mask).
3. part3.json per PARTS.md (title "Learning, heads, and the Transformer", chain from GUIDE3.md, sections s01..s19 with titles and data-lit, objects all seven, prev = Part 2 attention.html, next null, notation "part3").
4. sections3/sec01.html .. sec19.html per GUIDE3.md, one <section id="sNN"> + one <script> IIFE each, ids prefixed sNN-, using the CONTRACT components: companion prose (80 to 120 words) after each header and a .companion.after line; tables with named column headers wherever coordinates mean something (water, finance, person, glue, position; query/key axes; value axes); worksheets for every dot product, projection, softmax and loss; node sketches where the GUIDE says (FFN, LM head); frames with data-build and text/x-notes on every frame per PRESENT.md. Prose rules: plain verbs, sentence case, no em or en dashes, no "not just X but Y", no forced triples, no bold mini-heading lists, no AI vocabulary. Every number that the toy or the training block computes must come from AT.model / AT.forward / AT.train, never retyped.
5. Build and test: python3 assemble.py --part 3 --out ../part3.html; node qa.mjs ../part3.html --width 1280 --height 720 and --width 390 must report zero pageErrors/consoleErrors/katexErrors and overflowX false; node sweep.mjs ../part3.html no problems; node mathdiff.mjs sections3/secNN.html sections3/secNN.html no humanizer flags for every NN; present mode (?present) loads without errors. If headless Chromium cannot launch in your sandbox, verify in the system browser (serve the repo root with python3 -m http.server) and say so. Inspect at least six sections visually (screenshots or the live browser) and fix layout problems.
Final report: files created, the training numbers (loss before/after per eta, top probabilities), sections and frame counts, tests run with results, anything left undone.

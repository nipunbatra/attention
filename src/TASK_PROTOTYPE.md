Finish the presentation-mode prototype for Part 2 of an interactive teaching page. Work ONLY inside /Users/nipun/git/attention/src and write the assembled output to /Users/nipun/git/attention/attention.html. No network; do not install packages; do not commit or push.
Read first: HANDOVER.md (entry point), PRESENT.md (present-mode design), FRAMES.md (frame and build plan), CONTRACT.md (present-mode authoring syntax: .frame wrappers with data-title, data-build numbers, a script type="text/x-notes" per frame, data-autobuild="off").
State: the runtime in shell.html/shared.js implements present mode; sections/sec01.html to sec05.html carry hand-authored frames (sec05 was heavily revised afterwards with a mock search UI, so its frames need re-checking); sections/sec06.html has at most a single wrapper.
TASKS:
1. Re-author frames for sec05 and sec06 per FRAMES.md s05 and s06, adapted to the current content (mock search UI cards, key/value strips, the two spreadsheets, the hard/soft toggle), and check sec01 to sec04 frames still match their content. Every frame must stand alone in present mode; read mode content must not change; script-created elements need their data-build attributes too.
2. Verify s01 to s06 in present mode. If the bundled headless Chromium cannot launch in your sandbox (a previous run hit a Mach port error), use the system browser: serve the repo root with python3 -m http.server and open attention.html?present at 1920x1080, press ArrowRight through every build of every frame of s01 to s06, and inspect each frame: builds reveal in order, hidden builds not visible early, steppers advance with the arrow, nothing clipped, tall frames scroll inside, fragment CSS in px scales (use em or body.present #sNN rules per CONTRACT.md). Fix problems in the fragments. Check Esc returns to read mode, the hash deep link #s05/2/1 works, the overview (O) opens, the blank key works, and the presenter window (window.open) opens and syncs.
3. Read mode must stay clean: run node qa.mjs ../attention.html --width 1280 --height 720 and --width 390 (zero errors, overflowX false) and node sweep.mjs ../attention.html (no problems) if headless Chromium works; otherwise the equivalent in the system browser and say so.
4. Rebuild with python3 assemble.py --part 2 --out ../attention.html.
Prose rules: plain verbs, sentence case, no em or en dashes, no forced triples.
Final report: files changed, frames and builds per section for s01 to s06, what you inspected, remaining issues.

Rebuild Vision II of an interactive teaching series so it teaches as well as Part 2 (attention.html), which the instructor likes most: gentle introduction, strong motivation, lots of intuition, many good figures, worked examples, and one end-to-end run. Work ONLY inside /Users/nipun/git/attention/src and write the assembled output to /Users/nipun/git/attention/vision2.html. No network; no package installs; do not commit or push. Do not edit shell.html, shared.js, vision-shared.js, toy5.json, part5.js or any other part's sections; if you need a shared component change, add it to part6.js instead and note it in your report.
Read, in this order: HANDOVER.md (entry point, build and test commands), VISION_FEEDBACK.md (the diagnosis and the per-part plan; section 4 is your section list), VISION_AXES.md (the scene, the named axes, the shared components; authoritative), CONTRACT.md (the AT runtime; read the new "Vision components" section and the tables/worksheets/present-mode sections), PARTS.md, BRIEF.md sections 1, 3, 5, humanizer/SKILL.md (prose rules), then the Part 2 reference fragments to copy idioms from: sections/sec01.html (opening with a question and bars), sec05.html (the mock search UI), sec07.html (records view), sec08.html (routing tree with worksheets), sec09.html (the update centrepiece), sec10.html (the 2-D plot with Δe arrows), sec15.html (the stepper walkthrough), sec18.html and sec19.html (pause-and-think and summaries). Then the current sections6/ fragments and part6.js (raw material; rebuild rather than patch).
TARGET: Vision II: Learn visual representations without class labels (7 to 10 sections), 3,500 to 4,500 words, at least one figure per section that is not a table or a flow box, one end-to-end run with a loss curve, one caveat box after the hero and no caveat paragraphs at the end of sections, named column headers everywhere (no "coordinate N"), thumbnails as row labels, every number computed by the toy, frames with data-build and text/x-notes on every frame (PRESENT.md), companion prose of 80 to 120 words per section, a motivation opener with one memorable number, the scope line in the hero provenance.
SPECIFIC PLAN: First build toy6.json and part6.js per VISION_AXES.md section 4: the frozen Vision I encoder (from toy5.json) plus a tiny MAE decoder trained in a seeded numpy script train_vision2.py on scenes A, B, D with the right-mug patches hidden (export the training curve and the predictions at steps 0, 10, 100); DINO with two views (flip, brightness 0.75, crop) of scene A through the encoder plus a 3-slot head, with teacher logits COMPUTED from the encoder (not typed) and a centering on/off switch whose off state shows collapse (loss → log 3); I-JEPA predicting the hidden patch's (brightness, edge) feature; a linear probe on frozen features separating two-mug from one-mug scenes. Then follow the Vision II plan in VISION_FEEDBACK.md section 4 (sections 1 to 10: motivation with a sourced number; MAE idea with the triptych; MAE trained with the curve and triptychs at three steps and the ambiguous-patch case; which pixels enter the loss fed by the trained decoder; two views drawn with the single ramp and two points pulled together; DINO with the collapse toggle; I-JEPA as two points; the linear probe as a decision line in the brightness/edge plane; comparison table plus the caveat box; summaries).
Update part6.json (title, chain, sections, notation "vision2" rows for the new symbols, provenance line) and the notation rows in part6.js. Build and test: python3 assemble.py --part 6 --out ../vision2.html; node qa.mjs ../vision2.html --width 1280 --height 720 and --width 390 (zero errors, overflowX false); node sweep.mjs ../vision2.html (no problems); node mathdiff.mjs sections6/secNN.html sections6/secNN.html for every NN (no humanizer flags); a present-mode walk (walk.mjs) with no overflow; run the part's check script (check_vision2.mjs; update it to the new toy) and make it pass. If the bundled headless Chromium cannot launch in your sandbox, verify in the system browser (serve the repo root with python3 -m http.server) and say so. Inspect at least six sections visually and fix layout. Final report: sections and frame counts, the key numbers (attention rows, predictions, losses), files created or changed, tests run with results, anything left undone.

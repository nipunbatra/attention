Build Part 1 of a three-part single-file interactive teaching series (deep-learning students) in this repository. Work ONLY inside src/ and write the assembled output to the repository root as part1.html. No package installs; do not commit or push. Do not edit src/sections/*, src/sections3/*, src/shell.html or src/shared.js; put any runtime addition in src/part1.js (loaded after shared.js for Part 1 only).
Read first, in this order: HANDOVER.md (entry point, build and test commands, rules), PARTS.md (multi-part contract: assemble.py --part 1 uses sections1/, toy1.json, part1.json, part1.js), GUIDE1.md (the section plan for Part 1, authoritative), CONTRACT.md (runtime API: tables with named axes, dotCalc/matVecCalc worksheets, netSketch, stepper, reveal, callout, present-mode authoring with .frame/data-build/x-notes), BRIEF.md sections 1, 3, 5, and the Part 2 and Part 3 references for idioms: sections/sec01.html, sec03.html, sec07.html and sections3/sec02.html, sec08.html (companion prose, tables, worksheets, frames). The instructor's slide deck that Part 1 mirrors is /Users/nipun/git/dl-teaching/lecture11/mega/lecture1.typ (read it for the exact teaching sequence, examples and wording: the aabid name, w = 3, 27 classes, the six training rows, one-hot vs embedding, concatenate vs add, the MLP, softmax, the 0.223 vs 4.605 loss comparison, sample-append-repeat, tokenization, word tokens, the growing context question).
Deliverables:
1. train_names.py (numpy only, fixed seed): load names.csv (columns index,Name; lowercase, keep a-z only, dedupe), vocabulary {-, a..z} with '-' as boundary (id 0), w = 3, E 27x2 with a vowel-ness constraint on axis 1 (vowels positive, consonants negative, via a penalty), W_1 6x32, b_1, W_2 32x27, b_2, tanh hidden, cross-entropy, Adam, enough steps for plausible names (report train and held-out loss). Export toy1.json exactly as GUIDE1.md lists: vocab, E, W1, b1, W2, b2, w, axes {"e": ["vowel-ness", "learned axis 2"]}, the six aabid rows with their probabilities, a training curve (loss every 50 steps), the before-training parameters, 20 sampled names with the seed, and notes. Two-decimal numbers for display; keep full precision where the page computes.
2. part1.js extending window.AT with AT.mlp: embed(ids), concat(rows), forward(ids) -> {a0, a1, z, p}, loss(ids, target), generate({temperature, greedy, seed}) -> name with a per-step trace, tokenizeChars, tokenizeWords, tokenizeSubwords (a small illustrative merge table). A node script must confirm the JS forward matches train_names.py on the six aabid rows to 1e-6.
3. part1.json per PARTS.md (title "From characters to next-token prediction", chain from GUIDE1.md, sections s01..s16 with titles and data-lit, objects ["e"], prev null, next Part 2 attention.html, notation "part1").
4. sections1/sec01.html .. sec16.html per GUIDE1.md: one <section id="sNN"> + one <script> IIFE each, ids prefixed sNN-, CONTRACT components; companion prose (80 to 120 words) after each header and a .companion.after line; tables with named column headers (embedding axes; positions coloured in the concatenation; logits and probabilities as tables); worksheets for the one-hot dot products, one hidden unit, one logit, the softmax and the loss; AT.netSketch for the MLP and for the window-growth frame in s14 (w = 3, 5, 10, 100 with the live W_1 shape and parameter count, plus the d = 256, d_h = 1024 example giving about 26 million weights at w = 100); the name generator in s10 with greedy/sample and a temperature slider and a visible sliding window; the tokenizer widget in s12; frames with data-build and text/x-notes on every frame per PRESENT.md. Prose rules: plain verbs, sentence case, no em or en dashes, no "not just X but Y", no forced triples, no bold mini-heading lists, no AI vocabulary. Every number the model computes comes from AT.mlp / window.__TOY__, never retyped (the deck's illustrative numbers such as E[1] = (1.2, 0.1) are NOT used; use the trained numbers).
5. Build and test: python3 assemble.py --part 1 --out ../part1.html; node qa.mjs ../part1.html --width 1280 --height 720 and --width 390 must report zero errors and overflowX false; node sweep.mjs ../part1.html no problems; node mathdiff.mjs sections1/secNN.html sections1/secNN.html no humanizer flags; present mode (?present) loads. If headless Chromium cannot launch in your sandbox, verify in the system browser (serve the repo root with python3 -m http.server) and say so. Inspect at least six sections visually and fix layout problems. Finally update the Part 1 card in ../index.html and src/index_series.html (make the title a link to part1.html and replace the "Coming soon" span with two links: Open Part 1 and Present Part 1, in the same markup as the Part 2 card) and set "prev" in part2.json to Part 1 (part1.html) if it is not already, then rebuild Part 2 with python3 assemble.py --part 2 --out ../attention.html.
Final report: files created, the training losses and the 20 sampled names, the six aabid row probabilities, sections and frame counts, tests run with results, anything left undone.
